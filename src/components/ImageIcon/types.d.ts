export type ImageIconProps<T> = {
  focused?: boolean;
  // backIcon?: "entypo" | "back";
  // backPress?: () => void;
  // infoIcon?: boolean;
  // infoPress?: () => void;
  // leftContent?: React.ReactNode;  
  // rightContent?: React.ReactNode;  
};